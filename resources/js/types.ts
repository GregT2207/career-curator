export interface Listing {
    site: string;
    link: string;
    title: string;
    description: string;
    salary: number;
    salaryRange: string;
    level: string;
    travel: string;
};

export interface ListingLink {
    site: string;
    url: string;
};